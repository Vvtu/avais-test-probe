export const RESPONSE_DELAY = 300;

export const PROBABILITY_OF_ERROR = 0.3;

export const SORTING_PARAM = 'sorting';

// eslint-disable-next-line no-shadow
export enum ISortingParam {
  cheep = 'cheap',
  speed = 'speed',
  optimal = 'optimal',
}
